<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: {method: :post}) do |f| %>
    <%= devise_error_messages! %>


    <div class="page-forgot">

      <div class="signin-header">
        <section class="logo text-center">
          <h1><a href="#/">Passwort zurücksetzen</a></h1>
        </section>
      </div>

      <div class="form-container">

        <div class="additional-info text-center">
          <p class="text-small">
            Bitte gib deine E-Mail Adresse an, mit der du dich registriert hast. Wir senden dir eine Email mit
            deinem Nutzernamen und einem Link, um dein Passwort zurückzusetzen.
          </p>
        </div>

        <div class="form-container text-center">

          <form class="form-horizontal" name="forgotPasswordForm">
            <div class="form-group">
              <div class="input-group input-group-first">
                        <span class="input-group-addon">
                            <span class="ti-email"></span>
                        </span>
                <div class="field">
                  <%= f.email_field :email, autofocus: true, :class=>"form-control input-lg", placeholder:"E-Mail" %>
                </div>
              </div>
            </div>

            <div class="divider divider-lg"></div>
            <div class="form-group">
              <div class="actions">

              </div>
              <%= f.submit "Passwort zurücksetzen", :class => "btn btn-lg btn-block btn-primary" %></a>
            </div>
<% end %>
<%= render "devise/shared/links" %>
</form>

</div>
</div>
</div>