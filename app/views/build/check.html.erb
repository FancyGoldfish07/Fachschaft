
<% provide(:title,"Eventüberblick - Abschicken") %>
  <%= bootstrap_form_for @event, url: wizard_path do |f| %>

  <%= render @event %>
   <p>
        Mit einem Klick auf Weiter beginnt der Reviewprozess für dein Event.
      </p>
      <%= f.hidden_field :reviewed, { :value => true } %>
    <%= render partial: 'wizardControls', locals:{f: f} %>
  <% end %>
