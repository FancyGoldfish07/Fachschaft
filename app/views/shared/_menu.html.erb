
<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Fachschaft Wirtschaft', "http://www.fh-fachschaft.de/", class: "navbar-brand" %>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">

        <li class="dropdown">
          <%= link_to 'Kalender <span class="caret"></span>'.html_safe, '#', :id => 'nav_calendar', :'data-toggle' => 'dropdown', :class => 'dropdown-toggle' %>
          <ul class="dropdown-menu">
            <li><%= link_to 'Kalender', root_path %></li>
            <% if (current_user.isFachschaft || current_user.isAdmin || current_user.isManager) && current_user.present? then %>

                <% if current_user.isFachschaft then %>
                    <li><%= link_to 'Neues Event', new_build_path %></li>
                <%end %>
                <% if current_user.isAdmin then %>
                    <li><%= link_to 'Events genehmigen', :permittables %></li>
                <% end %>
                <% if current_user.isManager then %>
                    <li><%= link_to 'Events Veröffentlichen', :publishables %></li>
                <% end %>
            <% end %>
          </ul>
        </li>






      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><a href="#"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
        <li><a href="#"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
      </ul>
    </div>
  </div>
</nav>