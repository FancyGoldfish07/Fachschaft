


  <%= render 'calendar' %>

<br>
  <% if current_user.present? && current_user.isAdmin ||current_user.isFachschaft %>
<%= button_to_default "Neuen Eintrag erstellen", new_build_path %>
  <% end %>