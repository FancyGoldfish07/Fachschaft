<% provide(:title, "Kalendereinträge zum veröffentlichen") %>
<table class="table">
  <tbody>
  <% @events.each do |event| %>
      <tr><td><%= event.title %></td><td><%= button_to_small("Ansehen", review_path( event)) %></td></tr>
  <% end %>
  </tbody>
</table>