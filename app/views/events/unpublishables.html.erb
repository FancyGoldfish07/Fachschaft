<% provide(:title, "Kalendereinträge, die gelöscht werden sollen") %>
<table class="table">
  <tbody>
  <% @events.each do |event| %>
      <tr>
        <td><%= event.title %></td>
        <td><%= button_to_small("Ansehen", unpublish_path( event)) %></td>
      </tr>
  <% end %>
  </tbody>
</table>