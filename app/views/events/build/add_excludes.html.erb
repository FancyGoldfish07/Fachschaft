<div class="panel-heading">
  <h3>Termine</h3>
</div>
<div class="panel-body">
  <%= simple_form_for @recurrence, url: wizard_path do |f| %>
      <%= f.error_notification %>
      <p>Vom <%= @recurrence.start %> bis zum <%= @recurrence.end %> findet dieses Event an diesen Tagen statt.</p>
      <p>Bitte wähle nun die Tage aus, wo dieses Event nicht stattfindet (z. B. in den Semesterferien). </p>
      <% dates = @recurrence.getDatesStartFinish %>
<% dates.each do |date| %>
         <p><%= date %></p>
          <%end %>
      <%= f.input :exclude_ids, label: "Tage, an denen " + @recurrence.events.first.title + " nicht stattfindet", collection: dates.each_with_index.map {|name, index| [name,index]},input_html:{multiple: true}, selected: :exclude_ids %>

      <div class="actions">
        <%= f.submit "Continue" %>
      </div>
  <% end %>
</div>