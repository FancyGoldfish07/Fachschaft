<% if current_user.present? && (current_user.isAdmin || current_user.isManager) %>
<div class="panel-heading">
  <h3>Übersicht</h3>
</div>
<div class="panel-body">

  <%= render @newsletter %>

  <%= render :partial => "review_form", locals:{newsletter: @newsletter} %>

</div>

<%end%>